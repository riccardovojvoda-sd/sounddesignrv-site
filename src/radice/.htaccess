# sounddesignrv.com - Apache (Infomaniak)

# Forza HTTPS e www
RewriteEngine On
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} !^www\. [NC]
RewriteRule ^ https://www.sounddesignrv.com%{REQUEST_URI} [R=301,L]

# Lingua: solo la home, solo se il browser preferisce l'inglese e non si arriva da una pagina del sito
# (cosi' il selettore IT/EN funziona sempre). 302 e non 301: dipende da chi chiede, non e' permanente.
RewriteCond %{REQUEST_URI} ^/$
RewriteCond %{HTTP:Accept-Language} ^en [NC]
RewriteCond %{HTTP_REFERER} !sounddesignrv\.com [NC]
RewriteRule ^$ /en/ [R=302,L]
<If "%{REQUEST_URI} == '/'">
  Header append Vary Accept-Language
</If>

# Vecchi indirizzi Squarespace
Redirect 301 /portfolio /
Redirect 301 /blog /approfondimenti/
Redirect 301 /a-vision-for-tomorrow /progetti/arcadia-earth/
Redirect 301 /s/Riccardo-Vojvoda-Sound-Designer-Profile_2026_ITA.pdf /assets/Riccardo-Vojvoda-Sound-Designer-Profile_2026_ITA.pdf
RedirectMatch 301 ^/blog/.* /approfondimenti/

# Pagina 404
ErrorDocument 404 /404.html

# Compressione
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css text/plain text/xml application/javascript application/json image/svg+xml
</IfModule>
<IfModule mod_brotli.c>
  AddOutputFilterByType BROTLI_COMPRESS text/html text/css text/plain text/xml application/javascript application/json image/svg+xml
</IfModule>

# Cache: asset immutabili a lungo, HTML sempre rivalidato
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/html "access plus 0 seconds"
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"
  ExpiresByType image/avif "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType video/mp4 "access plus 1 year"
  ExpiresByType application/pdf "access plus 1 month"
</IfModule>
<IfModule mod_headers.c>
  <FilesMatch "\.(avif|webp|jpe?g|png|svg|woff2|mp4|js|css)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
  Header always set X-Content-Type-Options "nosniff"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Permissions-Policy "camera=(), microphone=(), geolocation=()"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set Content-Security-Policy "default-src 'self'; img-src 'self' data:; media-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; font-src 'self'; frame-src https://www.youtube-nocookie.com; connect-src 'self'; base-uri 'self'; form-action 'self' mailto:; upgrade-insecure-requests"
</IfModule>
AddType font/woff2 .woff2
AddType image/avif .avif
AddType video/mp4 .mp4
Options -Indexes
